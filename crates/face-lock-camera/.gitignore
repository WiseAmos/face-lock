# Build artifacts (regenerated by `napi build --release --strip`)
target/
Cargo.lock

# The .node binary is rebuilt per-platform by CI; we keep the source
# + scripts + the .d.ts/.js loader, but not the platform-specific binary.
face-lock-camera.*.node
